// Provedor ativo do player.
// false = Shinokai (padrão)
// true  = AnimeFire (modo de teste)
export const USE_ANIMEFIRE = true

// true: se o provedor principal falhar, tenta o outro automaticamente.
export const AUTO_FALLBACK_PROVIDER = true
