name: Build APK UpAnime+

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Java 17 (Obrigatório para evitar erro 65)
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Instalar dependências
        run: |
          npm install
          npm install @capacitor/core @capacitor/cli @capacitor/android

      - name: Build web (Vite)
        run: npm run build
        env:
          VITE_CONSUMET_URL: ${{ secrets.VITE_CONSUMET_URL }}

      - name: Inicializar Capacitor
        run: |
          npx cap add android || true
          npx cap sync android

      - name: Configurar Estilos (Fix NoActionBarLaunch)
        run: |
          # Garante que as pastas de recursos existem
          mkdir -p android/app/src/main/res/values
          
          # Cria o ficheiro de estilos com o tema que o Android está a pedir
          cat > android/app/src/main/res/values/styles.xml << 'XML'
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
                  <item name="android:windowFullscreen">true</item>
                  <item name="android:statusBarColor">@android:color/transparent</item>
                  <item name="android:navigationBarColor">@android:color/black</item>
              </style>
              <style name="AppTheme.NoActionBarLaunch" parent="AppTheme">
                  <item name="android:background">@null</item>
              </style>
          </resources>
          XML

      - name: Decodificar Keystore (Fix Tag Number 30)
        run: |
          # O parâmetro --ignore-garbage é vital para evitar corrupção de binários
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode --ignore-garbage > android/app/upanime.keystore

      - name: Configurar Assinatura no Gradle
        run: |
          # Remove blocos de assinatura antigos para evitar duplicados
          sed -i '/signingConfigs {/,$d' android/app/build.gradle
          
          # Adiciona a configuração limpa
          cat >> android/app/build.gradle << 'GRADLE'
          android {
              signingConfigs {
                  release {
                      storeFile file("upanime.keystore")
                      storePassword "upanime2026"
                      keyAlias "upanime"
                      keyPassword "upanime2026"
                  }
              }
              buildTypes {
                  release {
                      signingConfig signingConfigs.release
                      minifyEnabled false
                  }
              }
          }
          GRADLE

      - name: Build APK Release
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: upanime-plus-apk
          path: android/app/build/outputs/apk/release/app-release.apk
          
